# 2026-06-05 工作日志

## 本次目标

执行 SciPatent 第一轮可信协作优化，解决评估中发现的恢复上下文漂移、证据边界不清、科研图表复现路径不稳定和任务表无下一阶段 backlog 的问题。

## 已完成

- 统一 6-Gate 规则：
  - 更新项目 `AGENTS.md`、`MEMORY.md`、`docs/ARCHITECTURE.md` 和根级 `.agents/workflows/scipatent-lifecycle.md` 中的旧 4/5-Gate 表述。
  - 明确 Gate 1、Gate 2、Gate 2b、Gate 3、Gate 3b、Gate 4 为当前主控流程。
- 增加可信协作治理：
  - 新增 `docs/EVIDENCE_POLICY.md`，定义 `mock`、`described`、`verified`、`real` 四级证据。
  - 新增 `docs/DELIVERABLES.md`，定义成果包文件清单和收尾要求。
  - 新增 `docs/RUN_STATE.example.json`，提供生命周期运行状态样例。
  - 新增 `docs/RUN_STATE.current.json`，记录本轮优化产物、验证命令和剩余风险。
- 修复科研图表复现链路：
  - 将 `scripts/plot_figures.py` 的输出路径改为基于脚本位置解析，避免从项目目录执行时生成嵌套 `projects/` 目录。
  - 设置固定随机种子，提升图表可复现性。
  - 移除图 2 文本中的特殊字符，避免 matplotlib 导出时出现字体缺字警告。
  - 在 `package.json` 增加 `pnpm run figures` 复现入口。
- 更新任务系统：
  - 将 `memory/tasks.md` 从“暂无活跃任务”改为 Sprint 1/2/3 backlog。
  - 标记本轮已完成项，并保留文献核验、mock 标签、MCP 测试说明等后续任务。
- 标注现有测试产物：
  - 在 `docs/paper_draft.md`、`docs/patent_draft.md`、`docs/technical_disclosure.md`、`docs/ppt_outline.md` 顶部增加 `mock` 证据等级说明。
  - 明确这些产物仅用于流程演示和端到端测试，正式交付前必须替换为真实/核验数据并人工复核。

## 验证

- 已通过：`pnpm run figures`
  - 结果：成功生成 3 张 Nature-style 图表并输出到 `docs/figures/`。
  - 备注：沙箱内 `uv` 访问用户目录缓存受限，已在沙箱外验证通过。
- 已通过：`pnpm build`
  - 结果：Vite 冻结前端原型可正常构建。

## 后续风险

- `memory/mock_dataset.md` 仍为模拟数据，正式交付前必须替换为真实/核验数据。
- Phase 1 文献条目仍需逐条核验 DOI、年份和来源，尤其是跨年份或带 arXiv 备注的条目。
- `nature-academic-search` MCP 测试依赖 `pytest`，当前项目尚未写清本地测试环境准备方式。

---

## Science Workflow 商业化转向

### 本次目标

将项目主线从“论文 + 专利协作平台”调整为 “Science Workflow”：面向硕博个人的 SCI/期刊论文与硕博论文写作 Agent 工作流。煤化工/能源化工保留为第一垂直领域，专利能力降为可选 extension。

### 已完成

- 新建 `feature/science-workflow` 分支。
- 新增项目级主入口 `.agents/workflows/science-workflow.md`。
- 新增根级重定向入口 `.agents/workflows/science-workflow.md`。
- 重写 README、ARCHITECTURE、DELIVERABLES、EVIDENCE_POLICY、MEMORY，统一主叙事为 Science Workflow。
- 将 `scipatent-lifecycle.md` 调整为专利 extension，默认不进入。
- 将 `scipatent_config.json` 的专利转化默认开关设为 `false`。
- 新增模板：
  - `templates/real_data_intake.md`
  - `templates/citation_verification.md`
  - `templates/chapter_plan.md`
  - `templates/advisor_response.md`
  - `templates/revision_action_table.md`

### 关键决策

- 商业主线是 SCI/期刊论文与硕博论文写作，不是专利。
- 煤化工/能源化工不降级为普通示例，而是第一垂直领域包。
- 专利能力保留，不删除，但默认作为 extension。
- 第一阶段继续走 Agent Workflow，不做 Web SaaS。
